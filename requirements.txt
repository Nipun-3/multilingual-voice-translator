# Core dependencies for language translator
gtts
streamlit
googletrans==3.1.0a0

# Optional: Only needed for local microphone version
# pygame
# speechrecognition

# MLOps dependencies (for main_with_mlops.py)
mlflow>=2.8.0
pydantic>=2.0.0

